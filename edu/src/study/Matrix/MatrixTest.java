package study.Matrix;

public class MatrixTest {
    public static void main(String[] args) {
        
        // Matrix m1 = new Matrix(2, 3); // 2행 3열의 행렬을 랜덤으로 생성해라.
        // m1.showData();
        // Matrix m2 = new Matrix(2, 3);
        // m2.showData();

        // Matrix mm = Matrix.AddMatrix(m1, m2); // 두개의 행렬을 받아 더한 결과를 반환해라.
        
        Matrix<Integer> m1 = new Matrix<Integer>(2, 3, Integer.class);
    }
}
